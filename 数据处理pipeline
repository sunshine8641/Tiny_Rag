清晰的目标，**Tiny_RAG = 个人知识库 RAG 工具**。

数据摄入（Ingestion）是把各种资料 → 可检索的 chunks + embeddings。**核心 8 个功能**：

---

## 数据摄入功能清单

### 1. **Loader（格式支持）**

按你的资料类型定 MVP 范围：

| 类型 | 推荐工具 | MVP 是否必做 |
|---|---|---|
| PDF | pymupdf | ✓ 必做 |
| Markdown | 直接读 | ✓ 必做 |
| TXT | 直接读 | ✓ 必做 |
| EPUB（电子书）| ebooklib | 推荐 |
| 网页 URL | requests + readability | 推荐 |
| Notion 导出 | 直接读 .md | 推荐 |
| Obsidian vault | 直接读 .md | 推荐 |
| 代码仓库 | 按文件读，跳过 node_modules | 后期 |
| 图片 OCR | pytesseract / paddleocr | 后期 |
| 微信/聊天记录 | 解析 .db | 后期 |

**MVP**：PDF + Markdown + TXT 三个先跑通。

---

### 2. **Parser（文本提取）**

不只是抽文字，**要保留结构**：

```
PDF parser 需要做：
  - 抽文字（pymupdf.get_text()）
  - 保留页码（page_num）
  - 识别标题（字体大小判断）
  - 识别章节（heading 标记）
  - 表格尽量不丢（pymupdf 有 tables API）
  - 双栏自动判断（left/right 拼接）
  - 跳过页眉页脚

Markdown parser 需要做：
  - 读 raw 文本
  - 提取 frontmatter（title/author/date）
  - 解析标题层级（# ## ###）
```

**关键**：丢东西比慢更糟——宁可慢 2x 也要保住结构。

---

### 3. **Chunker（切分）**

最影响召回质量的一环：

```
推荐参数：
  - chunk_size = 512 tokens（约 2000 中文字）
  - chunk_overlap = 100 tokens（约 400 字）
  - 按段落/句子切（不切在句子中间）
  - 标题作为 metadata 附加（不要放进 chunk 内容）
  - 父子 chunk（可选）：parent=整章，child=段落，检索用 child，喂 LLM 用 parent
```

实现方式：
- **简单版**：langchain `RecursiveCharacterTextSplitter`（按 `\n\n` `\n` `.` 分）
- **进阶版**：按 token 数（tiktoken），不超 512

**坑**：切太细 → 召回准但 context 割裂；切太粗 → 召回噪声大。

---

### 4. **Metadata（元数据）**

每个 chunk 必须带：

```python
{
    "chunk_id": "uuid",          # 唯一 ID
    "source": "/path/to/book.pdf",  # 原始文件
    "filename": "book.pdf",      # 文件名
    "page": 42,                  # 页码（PDF）
    "title": "Chapter 3: ..."   # 章节标题
    "author": "张三",            # 作者
    "created_at": "2024-01-15",  # 入库时间
    "mtime": 1234567890,         # 文件修改时间（用于增量）
    "chunk_index": 15,           # 在该文件第几个 chunk
    "text": "...chunk 内容..."   # 实际文本
}
```

**关键**：**page/title 让你能在 GUI 里显示「这个答案来自哪本书哪一页」**，用户最需要这个。

---

### 5. **Embedding（向量化）**

```
选型：
  - bge-m3 (BAAI/中文 SOTA, 568M)         ← 推荐
  - bge-large-zh-v1.5 (BAAI, 326M)        ← 备选，小一点
  - text-embedding-3-small (OpenAI)       ← 云端

实现：
  - batch_size = 16-32 (按显存/CPU 调)
  - 第一次入库算全量
  - 后续增量用 cache（见 Sync）
  - 进度条
```

---

### 6. **Storage（向量存储）**

```
选型：
  - Chroma          ← 推荐，文件存储，开箱即用
  - Qdrant          ← 生产级，需要 Docker
  - FAISS           ← 最快，但需要自己管 metadata

Chroma 用法（最简单）：
  - client = chromadb.PersistentClient(path="./chroma_db")
  - collection = client.get_or_create_collection("books")
  - collection.add(ids=..., embeddings=..., metadatas=..., documents=...)
```

**关键**：用 Chroma 持久化，**重启不用重算**。

---

### 7. **Sync（增量更新）**

最容易被忽略、但每天都在用：

```
逻辑：
  1. 扫描目录，对每个文件算 hash（mtime 或 content hash）
  2. 对比上次入库的 hash 表：
     - 新文件 → 切分 + embedding + 写入
     - 修改文件 → 删旧 chunks + 重新切分
     - 未改文件 → 跳过
  3. 删了的文件 → 从 DB 删 chunks

存储：
  - .ingest_state.json 记录每个文件路径 + hash + mtime

进阶（可选）：
  - watch 模式（watchdog 库监听 fs 变化）
  - 自动重建
```

**没这个功能**，你每次新加一本 PDF 都要重算整个 corpus，1 小时+。

---

### 8. **CLI（命令行入口）**

MVP 至少 3 个命令：

```bash
# 入库
rag ingest <path>              # 单文件
rag ingest <dir>               # 目录
rag ingest <url>               # URL
rag ingest --watch <dir>       # 监听模式

# 看库状态
rag status                     # 总数 / 文件数 / 大小
rag list                       # 列出所有 source

# 删除
rag remove <path>              # 删某文件的所有 chunks
rag clear                      # 清空（带 --yes 确认）
```

---

## MVP 优先级（建议今天做的）

```
P0（必做，2-3 小时）:
  ✓ PDF + Markdown loader
  ✓ 按段落切分（512 token, overlap 100）
  ✓ metadata（filename, page, title）
  ✓ bge-m3 embedding
  ✓ Chroma 持久化
  ✓ 基础 CLI：ingest / status

P1（推荐，明天做）:
  ✓ 增量更新（mtime + skip）
  ✓ EPUB loader
  ✓ URL loader
  ✓ rag remove 命令

P2（可选，看时间）:
  ✓ watch 模式
  ✓ 图片 OCR
  ✓ 父子 chunk
```

---

## 第一个文件建议

从 `ingest.py` 开始，写这 4 个函数：

```python
def load_pdf(path) -> list[dict]:
    """返回 [{'text': ..., 'page': N, 'title': ...}, ...]"""

def chunk_text(text, chunk_size=512, overlap=100) -> list[str]:
    """按 token 切分，保持句子边界"""

def embed_chunks(chunks, model) -> list[list[float]]:
    """bge-m3 batch encoding"""

def upsert_to_chroma(chunks, embeddings, metadatas, db_path):
    """写入 Chroma，upsert 语义（id 已存在则更新）"""
```

写完跑：
```bash
python ingest.py ~/books/some_book.pdf
python status.py   # 看到 100+ chunks 入库
```

---

**要不要现在开写**？先把 `ingest.py` 的骨架打起来，4 个函数各 20-30 行，今天能跑通 PDF 入库。